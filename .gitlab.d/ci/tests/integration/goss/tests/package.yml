---
package:
  bash:               { installed: true }
  ca-certificates:    { installed: true }
  curl:               { installed: true }
  jq:                 { installed: true }
  openssh-client:     { installed: true }
